
Use this prompt with Copilot:

> **UI Change Only – Move Actions Column to the Left**
>
> Keep the existing UI, layout, styling, spacing, functionality, APIs, and business logic exactly as they are.
>
> **Only modify the Actions column.**
>
> ### Requirements
>
> * Move the **Actions** column from the **far right** to the **far left** of the table (before the S.No column).
> * Replace the existing Edit and Delete icons with a **single compact dropdown button** (down-arrow/chevron style).
> * When the button is clicked, display a dropdown containing only:
>
>   * ✏️ Edit
>   * 🗑️ Delete
> * The dropdown should open beside the clicked button and close when clicking outside or after selecting an option.
> * Reuse the existing Edit and Delete functionality without changing any business logic.
> * Keep the Actions column compact (approximately 48–60px wide).
> * If the table is horizontally scrollable, make the **Actions** column **sticky/frozen on the left** so it always remains visible.
> * Do not modify any other columns, table layout, spacing, typography, colors, responsiveness, APIs, routing, backend code, or application logic.
> * The implementation should be minimal, production-ready, and limited only to moving the Actions column to the left and replacing the visible icons with the dropdown button.
